import { Dancing_Script, Fraunces } from "next/font/google";

export const dancingScript = Dancing_Script({
  weight: "600",
  subsets: ["latin"],
});

export const fraunces = Fraunces({
  weight: "600",
  subsets: ["latin"],
});
